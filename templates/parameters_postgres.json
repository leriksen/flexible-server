{
  "active_directory_auth": {
    "value": "${active_directory_auth}"
  },
  "availability_zone": {
    "value": "${availability_zone}"
  },
  "backup_retention_days": {
    "value": "${backup_retention_days}"
  },
  "cluster_size": {
    "value": "${cluster_size}"
  },
  "cmk_assigned_identity": {
    "value": "${cmk_assigned_identity}"
  },
  "cmk_encryption_type": {
    "value": "${cmk_encryption_type}"
  },
  "cmk_key_uri": {
    "value": "${cmk_key_uri}"
  },
  "create_mode": {
    "value": "${create_mode}"
  },
  "geo_redundant_backup": {
    "value": "${geo_redundant_backup}"
  },
  "ha_mode": {
    "value": "${ha_mode}"
  },
  "identity_type": {
    "value": "${identity_type}"
  },
  "location": {
    "value": "${location}"
  },
  "name": {
    "value": "${name}"
  },
  "password_auth": {
    "value": "${password_auth}"
  },
  "public_network_access": {
    "value": "${public_network_access}"
  },
  "replication_role": {
    "value": "${replication_role}"
  },
  "sku_name": {
    "value": "${sku_name}"
  },
  "sku_tier": {
    "value": "${sku_tier}"
  },
  "source_server_id": {
    "value": "${source_server_id}"
  },
  "standby_availability_zone": {
    "value": "${standby_availability_zone}"
  },
  "storage_autogrow": {
    "value": "${storage_autogrow}"
  },
  "storage_iops": {
    "value": "${storage_iops}"
  },
  "storage_size_gb": {
    "value": "${storage_size_gb}"
  },
  "storage_throughput": {
    "value": "${storage_throughput}"
  },
  "storage_type": {
    "value": "${storage_type}"
  },
  "tenant_id": {
    "value": "${tenant_id}"
  },
  "version": {
    "value": "${version}"
  }
}
